# Plant Counselor — backend dependencies (mirrors pyproject.toml).
#   pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database — PostgreSQL 16 (psycopg 3 + pool)
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0

# Migrations
alembic>=1.13.0
sqlalchemy>=2.0.30

# Auth — bcrypt + HS256 session cookie
bcrypt>=4.1.0
pyjwt>=2.8.0

# Config & env
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# LLM (Google Gemini) — per-request user key
google-genai>=2.6.0

# Scheduling
apscheduler>=3.10.0

# Utilities
python-ulid>=2.0.0
